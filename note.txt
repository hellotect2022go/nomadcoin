go.mod : pacakge.json 같은거, requirements.txt

1일차 
blockchain
 -> 데이타는 메모리에 저장
type block struct {
	data     string
	hash     string
	prevHash string
}
결정론적 : 단방향 
"sexy" = h_fn(x) => "sdfasdfaasdafasdfsdfsd"

B1
 b1Hash = (data +"")

B2 
 b2hash = (data + b1Hash)

B3 
 b3hash = (data + b2 Hash)

B4 
 b4hash = (data + b3Hash)

persistence : DB

P2P
